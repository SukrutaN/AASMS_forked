#include <Arduino.h>

void setup() {
    // So the code here runs only once, this section is to be used to setup pins and configure the microcontroller
}

void loop() {
    // This section contains the code that runs on loop (hence the name of the function)
}
